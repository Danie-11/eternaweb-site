
// Update fallback plan defaults from 'Portfolio' to 'CV' to avoid re-linking to removed page
(function(){
  // replace occurrences where plan fallback was 'Portfolio'
  // Simple global search & replace in app.js content handled in separate file update
})();
