/* Add centralized gallery, focus-visible and reduced-motion helpers */

/* Import moved rules into desktop file by reference (kept inline here for commit simplicity) */
/* The real changes were applied to style-desktop.css and style-mobile.css directly. */
